# SwiftyNetwork
SwiftyNetwork is a lightweight Swift networking library that simplifies HTTP requests, handles authentication tokens, supports JSON decoding, multipart uploads, and unified response handling with status-aware results. Clean, async/await-friendly, and easy to integrate.
